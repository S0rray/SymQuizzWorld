{{ form_start(themeForm) }}
    {{ form_row(themeForm.name) }}
    {{ form_row(themeForm.difficulty) }}
    {{ form_row(themeForm.picture) }}
	<button type="submit" class="btn btn-primary">{{ button_label|default('Créer') }}</button>
{{ form_end(themeForm) }}